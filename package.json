{
    "scripts": {
        "build": "yarn encore dev",
        "watch": "yarn encore dev --watch",
        "serve": "yarn encore dev-server --port 3005 --disable-host-check"
    },
    "devDependencies": {
        "@symfony/webpack-encore": "^0.28.2"
    },
    "dependencies": {
        "three": "^0.112.1"
    }
}
